{
  "name": "expense-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cap:sync": "mkdir -p dist && cp index.html dist/ && npx cap copy",
    "cap:open": "npx cap open android",
    "cap:build": "mkdir -p dist && cp index.html dist/ && npx cap copy && npx cap open android"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/filesystem": "^7.1.8",
    "@capacitor/preferences": "^7.0.4",
    "@capacitor/share": "^7.0.4"
  }
}

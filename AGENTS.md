# test-opencode

Scratch/experimental repo. The real project is the **expense tracker** in `app/`.

## Expense Tracker (`app/index.html`)

- Single-file HTML app with embedded CSS/JS (Chart.js from CDN).
- All data in browser `localStorage` — no backend.
- Deployed to Vercel via git auto-deploy on push to `main` in `app/` repo (`paskaeu25/expense-tracker`).
- Live URL: https://app-ten-sage-zu9xxqf68o.vercel.app/
- Currency: **Euro (€)**, date format: **yyyy-mm-dd**.
- FAB (+) button for quick expense entry (no "Add" tab).
- Edit: ✎ button per item, shared form with `editingId` state.
- Categories by month: list grouped under month headers like "June 2026".
- Receipt uploads: styled "Choose images" button with file count, thumbs in list.
- Default categories: Groceries, Eating Out, Transport, Utilities, Entertainment, Health, Shopping, Travel, Other.
- Search & filter: keyword search + year/month dropdowns to browse past months.
- ½ split toggle: halves the entered amount (reverts on second tap).
- CSV/JSON export + JSON import for cross-device sync.
- The `inputs/` folder has raw CSV, conversion script, and seed JSON (gitignored).
- `june-expenses.json` is never committed (gitignored, removed from history).

## Workflow

- Always commit and push from `app/` directory.
- No build step — just edit `index.html`, commit, push.
- Vercel auto-deploys every push to `main`.
- **IMPORTANT**: After every change, immediately commit and push to `main`. The user tests on the live Vercel deployment, not locally.

## Android App (Capacitor)

- Android source is in `app/android/` (generated by Capacitor).
- After editing `index.html`, sync to Android:
  ```
  cp index.html dist/
  npx cap copy
  ```
  Or use the shorthand: `npm run cap:sync`
- To open Android Studio: `npm run cap:open` (or `npx cap open android`)
- Build APK in Android Studio: **Build → Build APK(s)**
- The `dist/` directory is a staging area for Capacitor (gitignored).
- `android/` is also gitignored — each dev runs `npx cap add android` once.
- `capacitor.config.json` and `package.json` are in `.gitignore` (not committed) because they are local dev tooling, not part of the deployed web app.
- Vercel ignores `android/`, `dist/`, `node_modules/` via `.vercelignore`.
